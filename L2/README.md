# Лабораторна робота № 1
## Мета роботи

Метою даної лабораторної роботи є дослідження ефективності існуючих методів фільтрації зображень, що були спотворені шумами різних типів.

## Хід роботи
### Завдання 1-2

Завантажити з бібліотеки MATLAB кілька тестових зображень. Відобразити вихідні зображення на екрані ПК.

У цьому завданні були завантажені тестові зображення з бібліотеки MATLAB за допомогою функції imread. Для відображення зображень була використана функція imshow. Команда figure створює нове вікно для кожного зображення, а функція title додає підпис до нього.

```
% Завдання 1-2
% Завантаження та відображення стандартних зображень
img1 = imread('eight.tif'); 
img2 = imread('moon.tif');  
img3 = imread('cameraman.tif');

figure, imshow(img1), title('eight.tif');
figure, imshow(img2), title('moon.tif');
figure, imshow(img3), title('cameraman.tif');
```

<img width="1004" height="480" alt="image" src="https://github.com/user-attachments/assets/d1c6eb9f-afeb-4530-b025-36ac2864a2c0" />

### Завдання 3

Здійснити процедуру зашумлення зображення нормальним білим шумом і імпульсною перешкодою з різною щільністю.

У цьому завданні до зображення були додані різні типи шуму за допомогою функції imnoise.

Гаусівський шум моделює випадкові зміни яскравості пікселів, а шум типу "salt & pepper" створює випадкові чорні та білі точки на зображенні. 

Було використано різну щільність імпульсного шуму.

```
% Завдання 3
% Здійснити процедуру зашумлення зображення нормальним білим
% шумом і імпульсною перешкодою з різною щільністю.

J_gauss = imnoise(img1,'gaussian',0,0.01);
J_sp1 = imnoise(img1,'salt & pepper',0.02);
J_sp2 = imnoise(img1,'salt & pepper',0.08);
```

### Завдання 4

Відобразити зашумлені зображення.

На цьому етапі були відображені зображення після додавання шумів. Можна побачити, що гаусівський шум змінює яскравість пікселів по всьому зображенню, а імпульсний шум проявляється у вигляді чорних і білих точок.

```
% Завдання 4
% Відобразити зашумлені зображення.

figure,imshow(J_gauss),title('Зображення з гаусівським шумом');
figure,imshow(J_sp1),title('Salt & Pepper шум (0.02)');
figure,imshow(J_sp2),title('Salt & Pepper шум (0.08)');
```

<img width="1004" height="292" alt="image" src="https://github.com/user-attachments/assets/50328b8e-3c10-4683-a821-307bb66c86d3" />

### Завдання 5

Виконати фільтрацію вихідних зображень лінійними фільтрами з використанням віконних фільтрів низьких і високих частот, і процедури imfilter(I,h).

Для фільтрації зображення була використана функція imfilter. Низькочастотний фільтр усереднює значення пікселів у вікні 3×3 і дозволяє зменшити шум. Високочастотний фільтр підсилює різкі переходи яскравості та робить контури більш чіткими.

```
% Завдання 5
% Виконати фільтрацію зображень лінійними фільтрами.
h_low = ones(3,3)/9;
I_low = imfilter(J_gauss,h_low);

h_high = [0 -1 0; -1 5 -1; 0 -1 0];
I_high = imfilter(img1,h_high);
```

### Завдання 6

Відобразити зображення після фільтрації.

Після застосування низькочастотного фільтра зображення стало більш згладженим і шум зменшився. Після високочастотної фільтрації контури об'єктів стали більш чіткими.

```
% Завдання 6
% Відобразити зображення після фільтрації.
figure,imshow(I_low),title('Після низькочастотної фільтрації');
figure,imshow(I_high),title('Після високочастотної фільтрації');
```

<img width="1004" height="409" alt="image" src="https://github.com/user-attachments/assets/b9977c4d-a853-4685-9d54-e564cbbb6992" />

### Завдання 7

Профільтрувати зашумлені зображення різними лінійними фільтрами.

Було виконано фільтрацію зображення з імпульсним шумом за допомогою низькочастотного фільтра. У результаті шум зменшився, але деякі точки все одно залишилися, оскільки лінійні фільтри не дуже ефективні для такого типу шуму.

```
% Завдання 7
% Профільтрувати зашумлені зображення різними лінійними фільтрами.
I_sp_low = imfilter(J_sp1,h_low);
figure,imshow(I_sp_low),title('Фільтрація salt & pepper шуму');
```

<img width="505" height="459" alt="image" src="https://github.com/user-attachments/assets/abc53da4-4d3f-42fb-b7be-e8c1cbf44b35" />

### Завдання 8

Виконати фільтрацію адаптивним вінерівським фільтром.

Було використано адаптивний вінерівський фільтр. Він аналізує локальні області зображення і підлаштовує свої параметри для кращого придушення шуму. Такий фільтр добре працює для гаусівського шуму.

```
% Завдання 8
% Виконати фільтрацію адаптивним вінерівським фільтром.
I_wiener = wiener2(J_gauss,[5 5]);
figure,imshow(I_wiener),title('Вінерівська фільтрація');
```

<img width="513" height="416" alt="image" src="https://github.com/user-attachments/assets/08508f3c-6cae-4aed-ba56-71787f1a58e4" />

### Завдання 9

Виконати фільтрацію медіанним фільтром.

Медіанний фільтр замінює значення пікселя медіаною сусідніх пікселів у вікні. Такий підхід дозволяє ефективно прибирати імпульсний шум типу "salt & pepper", не сильно розмиваючи зображення.

```
% Завдання 9
% Виконати фільтрацію медіанним фільтром.
I_median = medfilt2(J_sp1);
figure,imshow(I_median),title('Медіанна фільтрація');
```

<img width="502" height="384" alt="image" src="https://github.com/user-attachments/assets/06a25f42-39d0-484d-8ddc-2bbcf8c252a6" />

## Висновки
У ході лабораторної роботи було вивчено основні засоби MATLAB для роботи з цифровими зображеннями. Отримано практичні навички завантаження, аналізу та обробки зображень, побудови гістограм, підвищення контрастності та створення негативів.
Було встановлено, що зміна контрастності безпосередньо впливає на розподіл яскравостей зображення, що відображається у зміні гістограми.
Контрольні запитання

1.	Поясніть процедуру лінійної фільтрації зображення віконним фільтром.
    Лінійна фільтрація виконується шляхом переміщення спеціального маски по всьому зображенню. Для кожного пікселя обчислюється нове значення як зважена сума значень сусідніх пікселів, які потрапляють у це вікно. Вагові коефіцієнти визначаються елементами маски фільтра.

2.	Чому відрізняються “вікна фільтрів” для низькочастотної і високочастотної фільтрації?
    Вікна відрізняються, тому що вони виконують різні задачі. Низькочастотні фільтри усереднюють значення пікселів і згладжують зображення, тому їх маски містять додатні значення. Високочастотні фільтри підсилюють різкі перепади яскравості, тому їх маски мають як додатні, так і від’ємні значення.


3.	Як змінюється вигляд зображення після його фільтрації низькочастотними фільтрами?
    Після такої фільтрації зображення стає більш згладженим, зменшується кількість шуму, але дрібні деталі та різкі контури можуть трохи розмиватися.

4.	Як змінюється вигляд зображення після його фільтрації високочастотними фільтрами?
    Після високочастотної фільтрації контури об’єктів і дрібні деталі стають більш чіткими. Однак при цьому може також підсилюватися шум на зображенні.


5.	Для перешкод якого характеру більше підходить медіанна фільтрація, і чому?
Медіанна фільтрація найкраще підходить для імпульсних перешкод, наприклад шуму типу «salt & pepper». Це тому, що медіанний фільтр замінює значення пікселя медіанним значенням сусідніх пікселів і добре видаляє поодинокі дуже яскраві або дуже темні точки.





